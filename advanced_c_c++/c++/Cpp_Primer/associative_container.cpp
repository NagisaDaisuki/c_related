/*
    使用关联容器
*/
#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <numeric>
#include <functional>

#include <set>
#include <map>
#include <unordered_map>
#include <unordered_set>

/*
    关联容器和顺序容器有着根本的不同：
    关联容器中的元素是按照关键字来保存和访问的。
    与之相对，顺序容器中的元素是按照他们在容器中的位置
    来保存和访问的
*/

class AC
{
public:
    AC(){}
    ~AC(){}
public:
    
private:


};

int main()
{
  int is_ok = EXIT_FAILURE;

  
  
  is_ok = EXIT_SUCCESS;
  return is_ok;

}
